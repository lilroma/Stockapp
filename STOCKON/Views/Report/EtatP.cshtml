@{
    ViewBag.Title = "Etats";
}

<h2>EtatP</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<script type="text/javascript">
    function redi() {
        $('IdEmpresa').valueOf;
        window.pp^/
    }
</script>
@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Etat sur une période</legend>

        <div class="editor-label">
            @Html.Label("Plage")
        </div>
        <div class="editor-field">
            @Html.Telerik().DateTimePicker().Name("DateD").Value(DateTime.Now)
        </div>

        <div class="editor-field">
            @Html.Telerik().DateTimePicker().Name("DateF").Value(DateTime.Now)
            @Html.Hidden("id", Url.RequestContext.RouteData.Values["id"])
        </div>

        <p>
        
            <input type="submit" value="Afficher" onclick=""/>
        </p>
    </fieldset>
}

