@model IEnumerable<STOCKON.Models.Utilisateur>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nouvel utilisateur", "Create")
</p>
<table>
    <tr>
        <th>
            Login
        </th>
        <th>
            Client
        </th>
        <th>
            Fournissaeur
        </th>
        <th>
            Facture
        </th>
        <th>
            Article
        </th>
        <th>
            Mouvement_stock
        </th>
        <th>
            Etats
        </th>
        <th>
           Edition montants
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fournissaeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Facture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Article)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mouvement_stock)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Etats)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PFacture)
        </td>
        <td>
            @Html.ActionLink("Editer", "Edit", new {  id=item.Id_utilisateur }) |
            @Html.ActionLink("Details", "Details", new {  id=item.Id_utilisateur }) |
            @Html.ActionLink("Supprimer", "Delete", new {  id=item.Id_utilisateur })
        </td>
    </tr>
}

</table>
